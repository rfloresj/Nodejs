// Asynchronous Function
// Timers module
/* function showTimeOut(subject) {
  console.log(`Estoy aprendiendo ${subject}`);
}

setTimeout(showTimeOut, 5000, "Node.js"); //Higher order programming */

function sum(a, b) {
  console.log(a + b);
}

setTimeout(sum, 10000, 5, 6);
